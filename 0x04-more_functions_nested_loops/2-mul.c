#include "main.h"

/**
 * mul - The entry point of the function that multiplies two digits
 *
 * @a: first digit to be multiplied
 *
 * @b: second digit to be multiplied
 *
 * Return: returns the result of the multiplication of the two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
